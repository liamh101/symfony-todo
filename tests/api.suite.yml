actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
            url: /api
            depends: Symfony
            part: Json
        - Symfony:
              app_path: 'src'
              environment: 'test'