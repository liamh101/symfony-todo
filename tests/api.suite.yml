actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - \Helper\Generic
        - REST:
            url: /api
            depends: Symfony
            part: Json
        - Symfony:
              app_path: 'src'
              environment: 'test'